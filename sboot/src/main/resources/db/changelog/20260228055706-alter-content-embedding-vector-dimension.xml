<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="20260228055706-1" author="claude">
        <!-- Delete all existing embeddings (they were generated with 1536-dim OpenAI model,
             incompatible with 768-dim Ollama nomic-embed-text model) -->
        <sql>DELETE FROM content_embedding</sql>

        <!-- Drop the HNSW index before changing column type -->
        <sql>DROP INDEX IF EXISTS idx_content_embedding_hnsw</sql>

        <!-- Change vector dimension from 1536 to 768 for nomic-embed-text compatibility -->
        <sql>ALTER TABLE content_embedding ALTER COLUMN embedding TYPE vector(768)</sql>

        <!-- Recreate HNSW index with new dimension -->
        <sql>
            CREATE INDEX idx_content_embedding_hnsw
                ON content_embedding USING hnsw (embedding vector_cosine_ops)
                WITH (m = 16, ef_construction = 64)
        </sql>

        <rollback>
            <sql>DELETE FROM content_embedding</sql>
            <sql>DROP INDEX IF EXISTS idx_content_embedding_hnsw</sql>
            <sql>ALTER TABLE content_embedding ALTER COLUMN embedding TYPE vector(1536)</sql>
            <sql>
                CREATE INDEX idx_content_embedding_hnsw
                    ON content_embedding USING hnsw (embedding vector_cosine_ops)
                    WITH (m = 16, ef_construction = 64)
            </sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
